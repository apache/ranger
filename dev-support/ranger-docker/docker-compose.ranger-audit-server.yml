# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

services:
  # Audit Ingestor Service - Receives audits from plugins and produces to Kafka (REFACTORED)
  ranger-audit-ingestor:
    build:
      context: .
      dockerfile: Dockerfile.ranger-audit-ingestor
      args:
        - RANGER_BASE_IMAGE=${RANGER_BASE_IMAGE}
        - RANGER_BASE_VERSION=${RANGER_BASE_VERSION}
        - RANGER_VERSION=${RANGER_VERSION}
        - KERBEROS_ENABLED=${KERBEROS_ENABLED}
    image: ranger-audit-ingestor:latest
    container_name: ranger-audit-ingestor
    hostname: ranger-audit-server.rangernw
    networks:
      ranger:
        aliases:
          - ranger-audit-server.rangernw
    stdin_open: true
    tty: true
    depends_on:
      ranger-kdc:
        condition: service_started
      ranger-kafka:
        condition: service_started
    ports:
      - "7081:7081"
      - "7182:7182"
    environment:
      JAVA_HOME: /opt/java/openjdk
      AUDIT_INGESTOR_HOME_DIR: /opt/ranger/audit-ingestor
      AUDIT_INGESTOR_CONF_DIR: /opt/ranger/audit-ingestor/conf
      AUDIT_INGESTOR_LOG_DIR: /var/log/ranger/audit-ingestor
      AUDIT_INGESTOR_HEAP: "-Xms512m -Xmx2g"
      RANGER_VERSION: ${RANGER_VERSION}
      KERBEROS_ENABLED: ${KERBEROS_ENABLED}
      KAFKA_BOOTSTRAP_SERVERS: ranger-kafka.rangernw:9092
    volumes:
      - ./dist/keytabs/ranger-audit-server:/etc/keytabs
      - ranger-audit-ingestor-logs:/var/log/ranger/audit-ingestor
      - ranger-audit-ingestor-spool:/var/log/ranger/audit-ingestor/audit
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7081/api/audit/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Solr Consumer Service (Unified Dispatcher)
  ranger-audit-consumer-solr:
    build:
      context: .
      dockerfile: Dockerfile.ranger-audit-dispatcher
      args:
        - RANGER_BASE_IMAGE=${RANGER_BASE_IMAGE}
        - RANGER_BASE_VERSION=${RANGER_BASE_VERSION}
        - RANGER_VERSION=${RANGER_VERSION}
        - KERBEROS_ENABLED=${KERBEROS_ENABLED}
    image: ranger-audit-dispatcher:latest
    container_name: ranger-audit-consumer-solr
    hostname: ranger-audit-consumer-solr.rangernw
    stdin_open: true
    tty: true
    depends_on:
      ranger-kdc:
        condition: service_started
      ranger-kafka:
        condition: service_started
      ranger-solr:
        condition: service_started
      ranger-audit-ingestor:
        condition: service_healthy
    ports:
      - "7091:7090"
    command: ["solr"]
    environment:
      JAVA_HOME: /opt/java/openjdk
      AUDIT_CONSUMER_HOME_DIR: /opt/ranger/audit-dispatcher
      AUDIT_CONSUMER_CONF_DIR: /opt/ranger/audit-dispatcher/conf
      AUDIT_CONSUMER_LOG_DIR: /var/log/ranger/audit-dispatcher/solr
      AUDIT_CONSUMER_HEAP: "-Xms512m -Xmx2g"
      RANGER_VERSION: ${RANGER_VERSION}
      KERBEROS_ENABLED: ${KERBEROS_ENABLED}
      KAFKA_BOOTSTRAP_SERVERS: ranger-kafka.rangernw:9092
      SOLR_URL: http://ranger-solr.rangernw:8983/solr
    networks:
      - ranger
    volumes:
      - ./dist/keytabs/ranger-audit-consumer-solr:/etc/keytabs
      - ranger-audit-consumer-solr-logs:/var/log/ranger/audit-dispatcher/solr
      - ./scripts/audit-server/ranger-audit-consumer-solr-site.xml:/opt/ranger/audit-dispatcher/conf/ranger-audit-consumer-solr-site.xml
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7090/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # HDFS Consumer Service (Unified Dispatcher)
  ranger-audit-consumer-hdfs:
    build:
      context: .
      dockerfile: Dockerfile.ranger-audit-dispatcher
      args:
        - RANGER_BASE_IMAGE=${RANGER_BASE_IMAGE}
        - RANGER_BASE_VERSION=${RANGER_BASE_VERSION}
        - RANGER_VERSION=${RANGER_VERSION}
        - KERBEROS_ENABLED=${KERBEROS_ENABLED}
    image: ranger-audit-dispatcher:latest
    container_name: ranger-audit-consumer-hdfs
    hostname: ranger-audit-consumer-hdfs.rangernw
    stdin_open: true
    tty: true
    depends_on:
      ranger-kdc:
        condition: service_started
      ranger-kafka:
        condition: service_started
      ranger-hadoop:
        condition: service_healthy
      ranger-audit-ingestor:
        condition: service_healthy
    ports:
      - "7092:7090"
    command: ["hdfs"]
    environment:
      JAVA_HOME: /opt/java/openjdk
      AUDIT_CONSUMER_HOME_DIR: /opt/ranger/audit-dispatcher
      AUDIT_CONSUMER_CONF_DIR: /opt/ranger/audit-dispatcher/conf
      AUDIT_CONSUMER_LOG_DIR: /var/log/ranger/audit-dispatcher/hdfs
      AUDIT_CONSUMER_HEAP: "-Xms512m -Xmx2g"
      RANGER_VERSION: ${RANGER_VERSION}
      KERBEROS_ENABLED: ${KERBEROS_ENABLED}
      KAFKA_BOOTSTRAP_SERVERS: ranger-kafka.rangernw:9092
      HDFS_NAMENODE: ranger-hadoop.rangernw:9000
    networks:
      - ranger
    volumes:
      - ./dist/keytabs/ranger-audit-consumer-hdfs:/etc/keytabs
      - ranger-audit-consumer-hdfs-logs:/var/log/ranger/audit-dispatcher/hdfs
      - ./scripts/audit-server/ranger-audit-consumer-hdfs-site.xml:/opt/ranger/audit-dispatcher/conf/ranger-audit-consumer-hdfs-site.xml
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7090/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  ranger-audit-ingestor-logs:
  ranger-audit-ingestor-spool:
  ranger-audit-consumer-solr-logs:
  ranger-audit-consumer-hdfs-logs:

networks:
  ranger:
    name: rangernw
    external: true
