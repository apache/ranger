/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */

package org.apache.ranger.ha.service;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.MethodOrderer;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.TestMethodOrder;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mockito;
import org.mockito.junit.jupiter.MockitoExtension;

/**
 * @generated by Cursor
 * @description : Unit Test cases for HARangerService
 */
@ExtendWith(MockitoExtension.class)
@TestMethodOrder(MethodOrderer.MethodName.class)
public class TestHARangerService {
    @Test
    void testMock_negative_startThrowsCheckedException() throws Exception {
        HARangerService service = Mockito.mock(HARangerService.class);

        Mockito.doThrow(new Exception("mock-start")).when(service).start();

        Assertions.assertThrows(Exception.class, service::start);
    }

    @Test
    void testMock_positive_invokesStartAndStopOnMock() throws Exception {
        HARangerService service = Mockito.mock(HARangerService.class);

        service.start();
        service.stop();

        Mockito.verify(service).start();
        Mockito.verify(service).stop();
    }

    @Test
    void testStart_negative_implementationMayThrow() {
        HARangerService service = new ThrowingHarangerService(true, false);

        Assertions.assertThrows(Exception.class, service::start);
    }

    @Test
    void testStart_positive_invokesWithoutError() throws Exception {
        HARangerService service = new RecordingHarangerService();

        Assertions.assertDoesNotThrow(service::start);
    }

    @Test
    void testStop_negative_implementationMayThrow() {
        HARangerService service = new ThrowingHarangerService(false, true);

        Assertions.assertThrows(Exception.class, service::stop);
    }

    @Test
    void testStop_positive_invokesWithoutError() throws Exception {
        HARangerService service = new RecordingHarangerService();

        Assertions.assertDoesNotThrow(service::stop);
    }

    private static final class RecordingHarangerService implements HARangerService {
        @Override
        public void start() {
        }

        @Override
        public void stop() {
        }
    }

    private static final class ThrowingHarangerService implements HARangerService {
        private final boolean failStart;
        private final boolean failStop;

        private ThrowingHarangerService(boolean failStart, boolean failStop) {
            this.failStart = failStart;
            this.failStop  = failStop;
        }

        @Override
        public void start() throws Exception {
            if (failStart) {
                throw new Exception("start");
            }
        }

        @Override
        public void stop() throws Exception {
            if (failStop) {
                throw new Exception("stop");
            }
        }
    }
}
