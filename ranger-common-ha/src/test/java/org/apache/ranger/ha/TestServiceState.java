/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */

package org.apache.ranger.ha;

import org.apache.hadoop.conf.Configuration;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.MethodOrderer;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.TestMethodOrder;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.ArgumentMatchers;
import org.mockito.Mockito;
import org.mockito.junit.jupiter.MockitoExtension;

import java.lang.reflect.Field;

/**
 * @generated by Cursor
 * @description : Unit Test cases for ServiceState
 */
@ExtendWith(MockitoExtension.class)
@TestMethodOrder(MethodOrderer.MethodName.class)
class TestServiceState {
    @Test
    void testBecomingActive_negative_throwsWhenHaDisabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertNotNull(state);
        Assertions.assertThrows(IllegalStateException.class, state::becomingActive);
    }

    @Test
    void testBecomingActive_positive_setsBecomingActiveWhenHaEnabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.becomingActive();

        Assertions.assertEquals(ServiceState.ServiceStateValue.BECOMING_ACTIVE, state.getState());
    }

    @Test
    void testBecomingPassive_negative_throwsWhenHaDisabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertThrows(IllegalStateException.class, state::becomingPassive);
    }

    @Test
    void testBecomingPassive_positive_setsBecomingPassiveWhenHaEnabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.becomingPassive();

        Assertions.assertEquals(ServiceState.ServiceStateValue.BECOMING_PASSIVE, state.getState());
    }

    @Test
    void testGetInstance_negative_returnsNullWhenConstructionFails() throws Exception {
        resetServiceStateSingleton();

        Configuration bad = Mockito.mock(Configuration.class);

        Mockito.when(bad.get(ArgumentMatchers.anyString(), ArgumentMatchers.anyString()))
                .thenThrow(new RuntimeException("fail"));

        Assertions.assertNull(ServiceState.getInstance(bad));
    }

    @Test
    void testGetInstance_positive_singletonWhenHaDisabledStartsActive() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState first  = ServiceState.getInstance(configuration);
        ServiceState second = ServiceState.getInstance(configuration);

        Assertions.assertSame(first, second);
        Assertions.assertEquals(ServiceState.ServiceStateValue.ACTIVE, first.getState());
    }

    @Test
    void testGetState_negative_notNullAfterHaDisabledInit() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertNotNull(state.getState());
    }

    @Test
    void testGetState_positive_reflectsPassiveWhenHaEnabledByDefault() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertEquals(ServiceState.ServiceStateValue.PASSIVE, state.getState());
    }

    @Test
    void testIsInstanceInMigration_negative_falseWhenPassive() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertFalse(state.isInstanceInMigration());
    }

    @Test
    void testIsInstanceInMigration_positive_trueAfterSetMigration() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.setMigration();

        Assertions.assertTrue(state.isInstanceInMigration());
    }

    @Test
    void testIsInstanceInTransition_negative_falseWhenPassive() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertFalse(state.isInstanceInTransition());
    }

    @Test
    void testIsInstanceInTransition_positive_trueWhenBecomingActive() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.becomingActive();

        Assertions.assertTrue(state.isInstanceInTransition());
    }

    @Test
    void testSetActive_negative_throwsWhenHaDisabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertThrows(IllegalStateException.class, state::setActive);
    }

    @Test
    void testSetActive_positive_setsActiveWhenHaEnabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.setActive();

        Assertions.assertEquals(ServiceState.ServiceStateValue.ACTIVE, state.getState());
    }

    @Test
    void testSetMigration_negative_throwsWhenHaDisabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertThrows(IllegalStateException.class, state::setMigration);
    }

    @Test
    void testSetMigration_positive_setsMigratingWhenHaEnabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.setMigration();

        Assertions.assertEquals(ServiceState.ServiceStateValue.MIGRATING, state.getState());
    }

    @Test
    void testSetPassive_negative_throwsWhenHaDisabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "false");

        ServiceState state = ServiceState.getInstance(configuration);

        Assertions.assertThrows(IllegalStateException.class, state::setPassive);
    }

    @Test
    void testSetPassive_positive_setsPassiveWhenHaEnabled() throws Exception {
        resetServiceStateSingleton();

        Configuration configuration = haEnabledConfiguration();

        ServiceState state = ServiceState.getInstance(configuration);

        state.setActive();
        state.setPassive();

        Assertions.assertEquals(ServiceState.ServiceStateValue.PASSIVE, state.getState());
    }

    private static Configuration haEnabledConfiguration() {
        Configuration configuration = new Configuration(false);

        configuration.set("ranger.service.name", "svc");
        configuration.set("svc.server.ha.enabled", "true");
        configuration.set("svc.server.ha.ids", "a,b");

        return configuration;
    }

    private static void resetServiceStateSingleton() throws Exception {
        Field field = ServiceState.class.getDeclaredField("instance");

        field.setAccessible(true);
        field.set(null, null);
    }
}
